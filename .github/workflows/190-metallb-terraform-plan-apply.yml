name: "190-metallb"

on:
  workflow_dispatch:
  push:
    paths:
      - "190-metallb/**"
      - ".github/workflows/190-metallb-terraform-plan-apply*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: ${{ github.workflow }}
      download-secret-files: true
    secrets: inherit