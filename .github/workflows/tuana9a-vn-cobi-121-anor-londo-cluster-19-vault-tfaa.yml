name: "tuana9a/vn/cobi/121-anor-londo-cluster/19-vault"

on:
  workflow_dispatch:
  push:
    paths:
      - "tuana9a/vn/cobi/121-anor-londo-cluster/19-vault/**"
      - ".github/workflows/tuana9a-vn-cobi-121-anor-londo-cluster-19-vault-tfaa*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: ${{ github.workflow }}
      login-vault-in-cluster-sa: true
    secrets: inherit
