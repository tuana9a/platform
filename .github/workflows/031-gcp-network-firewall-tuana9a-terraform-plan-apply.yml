name: "031-gcp-network-firewall-tuana9a"

on:
  workflow_dispatch:
  push:
    paths:
      - "031-gcp-network-firewall-tuana9a/**"
      - ".github/workflows/031-gcp-network-firewall-tuana9a*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      WORKING_DIR: ${{ github.workflow }}
      GCP_SERVICE_ACCOUNT: terraform-compute-admin@tuana9a.iam.gserviceaccount.com
    secrets: inherit