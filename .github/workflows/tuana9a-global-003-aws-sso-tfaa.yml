name: "tuana9a-global-003-aws-sso"

on:
  workflow_dispatch:
  push:
    paths:
      - "tuana9a/global/003-aws-sso/**"
      - ".github/workflows/tuana9a-global-003-aws-sso*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      WORKING_DIR: tuana9a/global/003-aws-sso
      AWS_ASSUME_ROLE: arn:aws:iam::384588864907:role/github-workflow
    secrets: inherit
