name: "015-cloudflare-tunnel"

on:
  workflow_dispatch:
  push:
    paths:
      - "015-cloudflare-tunnel/**"
      - ".github/workflows/015-cloudflare-tunnel-terraform-plan-apply*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      WORKING_DIR: ${{ github.workflow }}
      download-secret-files: true
    secrets: inherit