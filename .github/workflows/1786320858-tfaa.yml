name: tuana9a/global/022-cloudflare-pages
'on':
  workflow_dispatch: null
  push:
    paths:
    - tuana9a/global/022-cloudflare-pages/*
    - .github/workflows/1786320858-tfaa.yml
    branches:
    - rock-n-roll
jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: tuana9a/global/022-cloudflare-pages
      login-vault: in-cluster-sa
    secrets: inherit
