name: 015-cloudflare-tunnel
'on':
  workflow_dispatch: {}
  push:
    paths:
    - 015-cloudflare-tunnel/*
    - .github/workflows/1789567841-tfaa.yml
    branches:
    - rock-n-roll
jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: 015-cloudflare-tunnel
      login-vault: in-cluster-sa
    secrets: inherit
