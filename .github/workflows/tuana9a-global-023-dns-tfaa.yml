name: "tuana9a-global-023-dns"

on:
  workflow_dispatch:
  push:
    paths:
      - "tuana9a/global/023-dns/**"
      - ".github/workflows/tuana9a-global-023-dns*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      install-sops: true
      WORKING_DIR: tuana9a/global/023-dns
      AWS_ASSUME_ROLE: arn:aws:iam::384588864907:role/github-workflow
    secrets: inherit
