name: "checks"

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-terraform-key:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: check-terraform-key
        run: ./scripts/check_terraform_key.sh
  check-terraform-fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: check-terraform-fmt
        run: ./scripts/check_terraform_fmt.sh
