name: "tuana9a/vn/cobi/121-anor-londo-cluster/28-secretstores"

on:
  workflow_dispatch:
  push:
    paths:
      - "tuana9a/vn/cobi/121-anor-londo-cluster/28-secretstores/**"
      - ".github/workflows/tuana9a-vn-cobi-121-anor-londo-cluster-28-secretstores-tfaa*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: ${{ github.workflow }}
      login-vault: "in-cluster-sa"
    secrets: inherit
