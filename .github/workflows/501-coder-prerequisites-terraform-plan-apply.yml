name: "501-coder-prerequisites"

on:
  workflow_dispatch:
  push:
    paths:
      - "501-coder-prerequisites/**"
      - ".github/workflows/501-coder-prerequisites-terraform-plan-apply*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/terraform-plan-apply-notify.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: ${{ github.workflow }}
    secrets:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}