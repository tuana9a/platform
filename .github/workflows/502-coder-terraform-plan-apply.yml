name: "502-coder"

on:
  workflow_dispatch:
  push:
    paths:
      - "502-coder/**"
      - ".github/workflows/502-coder-terraform-plan-apply*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: ${{ github.workflow }}
    secrets: inherit