github_workflow:
  "on":
    workflow_dispatch:
      inputs:
        runs-on:
          type: string
          required: true
  jobs:
    main:
      with:
        login-vault-in-cluster-sa: true
github_workflow_opts:
  push: false
