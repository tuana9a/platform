name: "024-email"

on:
  workflow_dispatch:
  push:
    paths:
      - "024-email/**"
      - ".github/workflows/024-email-terraform-plan-apply*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      WORKING_DIR: ${{ github.workflow }}
      download-secret-files: true
    secrets: inherit