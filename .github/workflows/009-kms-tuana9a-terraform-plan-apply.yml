name: "009-kms-tuana9a"

on:
  workflow_dispatch:
  push:
    paths:
      - "009-kms-tuana9a/**"
      - ".github/workflows/009-kms-tuana9a*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      WORKING_DIR: ${{ github.workflow }}
      AWS_ASSUME_ROLE: arn:aws:iam::384588864907:role/github-workflow
    secrets: inherit
