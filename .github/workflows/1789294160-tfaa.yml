name: tuana9a/global/024-email-routing
'on':
  workflow_dispatch: {}
  push:
    paths:
    - tuana9a/global/024-email-routing/*
    - .github/workflows/1789294160-tfaa.yml
    branches:
    - rock-n-roll
jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: tuana9a/global/024-email-routing
      login-vault: in-cluster-sa
    secrets: inherit
