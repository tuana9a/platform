name: "195-vault-k8s-cobi-tuana9a"

on:
  workflow_dispatch:
  push:
    paths:
      - "195-vault-k8s-cobi-tuana9a/**"
      - ".github/workflows/195-vault-k8s-cobi-tuana9a-terraform-plan-apply*"
    branches:
      - rock-n-roll

jobs:
  main:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/tfaa.yml
    with:
      runs-on: self-hosted-0
      WORKING_DIR: ${{ github.workflow }}
      download-secret-files: true
    secrets: inherit