apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-node-exporter-full
  labels:
    grafana_dashboard: "1"
data:
  dashboards.yaml: |
    default:
      node-exporter-full:
        gnetId: 1860
        revision: 2
        datasource: prometheus
